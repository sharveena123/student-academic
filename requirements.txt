--only-binary :all:  # Force pre-built wheels (avoids source builds)
streamlit==1.33.0
pandas>=2.1.0        # Requires Python ≥3.8, compatible with 3.13
plotly==5.18.0
numpy>=1.26.0        # Modern version for Python 3.13